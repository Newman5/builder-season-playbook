# X Update Review

The X workflow is now manual-search based instead of submission-file based.

Use:

- `config/repos.yml` for builder X handles
- the dashboard at `/dashboard/` and builder pages at `/builders/{id}/` to open `from:{handle} pieceofpie` searches

This keeps X review lightweight and avoids X API costs.
